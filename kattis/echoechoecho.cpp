#include <iostream>
#include <iomanip>
#include <cmath>
#include <string>
#include <cstring>
#include <sstream>
#include <algorithm> 
#include <vector>
#include <unordered_map>

#define ll long long

using namespace std;

int main() {
    string word; cin >> word;

    printf("%s %s %s\n", word.c_str(), word.c_str(), word.c_str());
}